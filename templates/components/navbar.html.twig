<nav class="navbar navbar-expand-lg navbar-light bg-white">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="{{ path("app_home") }}">
            <img src="{{ asset("images/logo-e.png") }}" alt="e-TAX logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path("app_home") }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_apropos' ? 'active' : '' }}" href="{{ path("app_apropos") }}">A propos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_contact' ? 'active' : '' }}" href="{{ path("app_contact") }}">Contacts</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_prospect_index' ? 'active' : '' }}" href="{{ path("app_prospect_index") }}">Prospect</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_message_index' ? 'active' : '' }}" href="{{ path("app_message_index") }}">Message</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-bold {{ app.request.get('_route') == 'app_logout' ? 'active' : '' }}" href="{{ path("app_logout") }}">Logout</a>
                    </li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link btn btn-custom fw-bold {{ app.request.get('_route') == 'app_login' ? 'active' : '' }}" href="{{ path("app_login") }}">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
<style>
    .btn-custom {
        background-color: #005E55;
        color: white !important;
        border: none;
        padding: 0.375rem 0.75rem;
        border-radius: 0.25rem;
        transition: background-color 0.15s ease-in-out;
    }

    .btn-custom:hover {
        background-color: #004540;
        color: white;
    }

    .btn-custom.active {
        background-color: #003530;
        color: white;
    }

    .nav-item {
        display: flex;
        align-items: center;
    }
</style>